
        <h1 class="page-header">Events</h1>
        {{#if admin}}
            <a href="/addEvent" class="btn btn-primary "><b>Add Event</b></a>
        {{/if}}

        <div class="img-responsive">
    {{#unless admin}}
      {{#data}}
       <div id="postContainer">
          <form action='/Events/remove/{{id}}' method='POST'>
            <div id="event" class="col-sm-12 col-md-12 col-lg-12" >
              <div  class="well">
              <div class="caption">
                  <h3>{{Title}}</h3>
                  <p>{{Description}}</p>
                  <p>Posted on: {{Date}}</p>
                  <!-- <p><a href="#" class="btn btn-primary" role="button">Button</a> <a href="#" class="btn btn-default" role="button">Button</a></p> -->
                </div>
              </div>
            </div>
        </form>
</div>
    {{/data}}
    {{/unless}}

{{#if admin}}
  {{#data}}
    <div id="postContainer">
      <form action='/Events/remove/{{id}}' method='POST'>
          <div class="col-sm-12 col-md-12 col-lg-12" >
            <button type="submit" id="btnRemove" class="glyphicon glyphicon-remove btn btn-primary pull-right" ></button>

            <div class="well">
              <div class="caption">
                <h3>{{Title}}</h3>
                <p>{{Description}}</p>
                <p>Posted on: {{Date}}</p>
              </div>

            </div>
          </div>
          <hr>
      </form>
    </div>
  {{/data}}
{{/if}}

  </div>
  <div class="row col-md-12" />
  
